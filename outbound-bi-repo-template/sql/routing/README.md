# Routing

Build stages: deduplicated demand → normalized FC/ZIP/station coverage → carrier/service eligibility → CPT/TNT rules → cost/service selection → modeled vs actual allocation. Preserve a reason code at each step. Migrate the approved WMS translation here; no WMS implementation is supplied.
