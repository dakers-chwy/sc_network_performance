# Tableau workbooks

Store approved `.twb` source files here when permitted, alongside a short workbook README. Packaged `.twbx` and extract `.hyper` files are ignored by default because they can contain underlying data.

Document published URL, owner, data sources, output tables, refresh dependency, filters, calculated fields, denominator definitions, timezone, and validation screenshots or aggregate evidence. Inspect connection metadata before committing a workbook.
